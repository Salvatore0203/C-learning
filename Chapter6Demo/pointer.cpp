#include <iostream>

using namespace std;

int main()
{
	//指针是一个值为内存地址的 变量（或数据对象）——— 指针就是一个变量
	//内存地址一般用16进制表示，每台电脑运行同一个程序时的内存地址都不一样。

	//int* p;       定义一个指针p
	//p = $year;    赋值地址
	//基本用法：
	//数据类型*指针变量名;
	//例如：int* ptr_num;
    //      char* ptr_name;
    //      float* money_ptr;
    //      double* p_price;
    //      声明中的*和使用中的*是不一样的...

    //int num = 1024;
    //int* ptr_num;         *指针类型的‘定义’
    //ptr_num = &num;
    //*ptr_num = 1111;      *在这里是‘取值’

}
